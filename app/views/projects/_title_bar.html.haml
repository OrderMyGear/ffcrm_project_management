#confirm{ hidden }
.title_tools#menu
  = link_to_inline(:edit_project, edit_project_path(@project), :text => t(:edit)) + " | "
  = link_to_function(t(:delete) + '?', confirm_delete(@project))
.title_tools#buttons
  = view_buttons
.title#edit_project_title
  = h(@project.name)
  = image_tag("loading.gif", :size => :thumb, :id => "loading", :style => "display: none;")

%div#edit_project{ hidden }
